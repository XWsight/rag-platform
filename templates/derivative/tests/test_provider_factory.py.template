from __future__ import annotations

import unittest
from unittest.mock import patch

from rag_system.provider_factory import verify_offline_provider_factory

from {{PACKAGE_NAME}}.provider_factory import {{FACTORY_CLASS}}


class ProviderFactoryTests(unittest.TestCase):
    def test_factory_builds_the_base_contract_without_network_access(self) -> None:
        with patch.dict("os.environ", {}, clear=True), patch(
            "socket.socket.connect",
            side_effect=AssertionError("provider factory attempted a network connection"),
        ):
            verify_offline_provider_factory({{FACTORY_CLASS}}())


if __name__ == "__main__":
    unittest.main()
